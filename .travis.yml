sudo: required
services:
  - docker

before_install: 
  - docker build -t yvalain/react-test -f ./client/Dockerfile.dev ./client

script: 
  - docker run -e CI=true yvalain/react-test npm test

after_success:
  - docker build -t yvalain/multi-client -f ./client
  - docker build -t yvalain/multi-nginx -f ./nginx
  - docker build -t yvalain/multi-server -f ./server
  - docker build -t yvalain/multi-worker -f ./worker
  